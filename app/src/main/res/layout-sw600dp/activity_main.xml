<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/shown_count"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#ffff00"
        android:gravity="center"
        android:text="@string/zero"
        android:textAlignment="center"
        android:textColor="@color/colorPrimary"
        android:textSize="200sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/button-toast" />

    <Button
        android:id="@+id/button-toast"
        android:layout_width="638dp"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:onClick="shownToast"
        android:text="@string/Toast_test"
        android:textColor="@android:color/white"
        android:textSize="60sp"
        tools:layout_editor_absoluteX="1dp"
        tools:layout_editor_absoluteY="16dp" />

    <Button
        android:id="@+id/button_count"
        android:layout_width="639dp"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:onClick="countUp"
        android:text="@string/count_text"
        android:textColor="@android:color/white"
        android:textSize="60sp"
        app:layout_constraintBaseline_toBaselineOf="@+id/button-toast"
        tools:layout_editor_absoluteX="640dp" />


</androidx.constraintlayout.widget.ConstraintLayout>